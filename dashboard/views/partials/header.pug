mixin header(show)
  header
    a(href=env.APP_PATH+"dashboard").logo Dnour &nbsp;
      span | admin panel
      if(show)
        nav
          ul
            //- li 
            //-   a(href="/dashboard") List
            //- li
            //-   a(href="/create") Create new
            if(isAuthenticated)
              li 
              a(href=env.APP_PATH+"logout") Logout
            else
              li 
              a(href=env.APP_PATH+"login") Login

    div.notification
      h4.
        Notification
      p.
        Message in notification